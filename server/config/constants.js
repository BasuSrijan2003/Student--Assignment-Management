// Keep this simple and hardcoded for now — a school admin managing these
// dynamically would be a nice future upgrade, but is not needed for the MVP.
const CLASSES = ["6", "7", "8", "9", "10", "11", "12"];
const SECTIONS = ["A", "B", "C", "D"];

module.exports = { CLASSES, SECTIONS };
